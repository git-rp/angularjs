import { HighlighGreenDirective } from './highligh-green.directive';

describe('HighlighGreenDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlighGreenDirective();
    expect(directive).toBeTruthy();
  });
});
